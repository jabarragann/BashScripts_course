
#Search for regular expressions in a file. (Search for scott in auth.log)

grep scott auth.log 

#-i option make grep case insensitive

grep -i break-in auth.log

#awk-print the 12th expression of auth.log

grep -i break-in auth.log  | awk {'print $12'}

#homework check sed and awk manuals with man

#Get only the response time of www.google.com

ping -c 2 www.google.com | grep 'bytes from' | cut -d = -f 4

#Make a file excutable
cp -v * tempFolder/ 1>../success.txt 2>../error.txt

